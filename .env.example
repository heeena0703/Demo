# Copy to .env and fill values before deploying
SECRET_KEY=change-me-to-a-secure-random-string
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=virtual_classroom
# If you want to force sqlite set USE_SQLITE=true
USE_SQLITE=false
